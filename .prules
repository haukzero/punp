/*
 * Rules:
 * - Comment: 
 *     - line comment: // This is a line comment
 *     - block comment: /* This is a block comment */
 * - Add rule: REPLACE(FROM "old text", TO "new text");
 * - Delete rule: DEL(FROM "old text");
 * - Protected region: PROTECT(START_MARKER "start", END_MARKER "end");
 * - Clear rules: CLEAR();
 */

// Default rules
REPLACE(FROM "。", TO ". ");
REPLACE(FROM "，", TO ", ");
REPLACE(FROM "、", TO ", ");
REPLACE(FROM "；", TO "; ");
REPLACE(FROM "？", TO "? ");
REPLACE(FROM "！", TO "! ");
REPLACE(FROM "：", TO ": ");
REPLACE(FROM "“", TO "\"");
REPLACE(FROM "”", TO "\"");
REPLACE(FROM "‘", TO "'");
REPLACE(FROM "’", TO "'");
REPLACE(FROM "（", TO "(");
REPLACE(FROM "）", TO ")");
REPLACE(FROM "【", TO "[");
REPLACE(FROM "】", TO "]");

// Add your new rules here, the new will overwrite the old
